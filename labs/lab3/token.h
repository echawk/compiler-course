/*
  Here we are creating an enum 'tok' that stores the internal values
  for our different token types
*/
enum tok{
	IF,
	LET,
	IDENT,
	FUNC,
	ASSIGN,
	INT_LIT,
	SEMI,
	PLUS,
	MINUS,
	ASTERISK,
	DIV,
	LPAREN,
	RPAREN,
	ILLEGAL
};

